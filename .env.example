# Workspace-level Mongo settings used by the local DB scripts and optional state API.
# Copy this file to `.env` at the repo root when running Mongo-backed tooling locally.
NIRA_MONGODB_URI=mongodb://127.0.0.1:27017
NIRA_MONGODB_DB_NAME=nira_db
